# if the duration of the audio is more than chunklen_sec, it will be truncated
truncation: rand_trunc # rand_trunc, fusion
# if the duration of the audio is less than chunklen_sec, it will be filled
data_filling: repeatpad # repeatpad, repeat, pad 

sample_rate: 24000
nfft: 1024
n_mels: 64
hoplen: 240
window: hann

chunklen_sec: 10

train_dataset:
  sClotho: [train,valid]
  sClotho_ColRIR_New: [train,valid]
  sAudioCaps: [train,valid]
  sAudioCaps_ColRIR_New: [train,valid]

valid_dataset:
  sClotho_ColRIR_New: [test]
  sAudioCaps_ColRIR_New: [test]
  # sClotho_ColRIR: [test]
  # sAudioCaps_ColRIR: [test]

test_dataset:
  sClotho: [test]
  # sAudioCaps: [test]
